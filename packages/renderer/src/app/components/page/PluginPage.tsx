import { Component, createSignal } from "solid-js";
import { Row } from "../base/Flex";

interface Props {}

const Plugin: Component<Props> = (props: Props) => {
  return <Row></Row>;
};

export default Plugin;
