import { Component, createSignal } from "solid-js";
import { Row } from "../base/Flex";

interface Props {}

const Setting: Component<Props> = (props: Props) => {
  return <Row></Row>;
};

export default Setting;
