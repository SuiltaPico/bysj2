import { Component, createSignal } from "solid-js";
import { Row } from "../base/Flex";

interface Props {}

const DocumentManager: Component<Props> = (props: Props) => {
  return <Row></Row>;
};

export default DocumentManager;
